# 固件版本记录 (Firmware Changelog)

版本号定义在 [src/main.cpp](src/main.cpp) 的 `FW_VERSION` 宏。`?` 命令和上电 banner 都会打印当前版本。
每次改动固件行为时递增版本号并在此登记。

- **MAJOR** — 不兼容的协议/命令/输出格式变更
- **MINOR** — 新增命令或功能,向后兼容
- **PATCH** — bug 修复、内部实现调整,行为不变

---

## Piezo VBR-Sen ver1.5.0 — 2026-09-07

- 0% 基线的挡位选择从编译期常量改成**串口交互命令**:10s 标定一结束就打印
  `ADC calibrated: ...` + 1~5 挡菜单(每挡 = 标定均值 - N×1mV),发 `1`~`5` + 回车
  当场选定并转入正式采集;新增 `AdcPhase::AWAITING_LEVEL` 状态,选档前采样定时器停着
- `?` 帮助加了这条命令说明;`adc:` 状态行也会显示"等待选档"

## Piezo VBR-Sen ver1.4.0 — 2026-09-07

- 标定期间的调试打印加上时间戳:`YYYY-MM-DD HH:MM:SS  raw=... v=...V`
- `ADC_IDLE_MARGIN_V`(0% 基线的安全余量)改成 5 档可选(`ADC_IDLE_MARGIN_LEVEL`,0~4),
  范围 -0.001V(最灵敏)~ +0.005V(最保守,=原来的固定值),应对标定噪声实测为 0 的情况

## Piezo VBR-Sen ver1.3.2 — 2026-09-07

- 心跳 LED 翻转周期从 0.5s 改为 **1s**(`LED_INTERVAL_MS` 500→1000)

## Piezo VBR-Sen ver1.3.1 — 2026-09-07

- 修一个小问题:'s'/'cal' 触发标定后,第一行调试打印会在 ADC 还没落地第一个采样前就
  打出 `raw=0 v=0.000V`;改成从触发那一刻起等一个 `CAL_MONITOR_MS` 周期再打印
- `ADC_NOMINAL_IDLE_V` 从 3.188V 改为 **3.130V**(之前的 3.188V 可能是测量偏差)
- WARNING 提示改成两行:第一行是漂移数值,第二行单独一行 `- still usable but check sensor/wiring`

## Piezo VBR-Sen ver1.3.0 — 2026-09-07

- 标定调试打印(`raw=... v=...V`)的电压精度从 2 位小数改为 **3 位小数**(`v.vvvV`)
- `s` 命令触发的 10s 标定期间,现在也会像 `cal` 命令一样打印 `raw=... v=...V`
  (之前只有单独发 `cal` 才会打印,`s` 触发的标定是静默的)

## Piezo VBR-Sen ver1.2.1 — 2026-09-07

- 修复 bug:开机自动标定 + 持续打印 `raw=.../v=...V`(每 200ms 一行)把串口刷屏了,
  导致 `s`/`p`/`?` 等命令的回应都被冲没、看起来像没反应。**去掉开机自动标定**,
  重新回到原来的行为:开机只等 `T` 命令设置时间(空格快捷键仍然保留)
- `cal` 命令的 raw/电压打印**只在 10s 标定窗口内**输出,标定一结束(转入正常状态)
  就自动停,不再无限打印到 's'/'p'

## Piezo VBR-Sen ver1.2.0 — 2026-09-07

- 新增 `cal` 命令:纯校准调试模式,不需要先设时钟。做一次 10s 的 0% 标定,标定期间和
  之后都由 `loop()` 每 200ms 打印一行 `raw=vvvv v=a.aaV`,直到收到 `s`/`p` 才停
- **开机/复位自动做一次同样的标定 + 持续打印**(不用等命令),方便台架测试;
  发 `s` 正式开始采集,或发 `p` 停止,都会退出这个调试模式

## Piezo VBR-Sen ver1.1.0 — 2026-09-07

- 新增测试快捷键:串口发一个空格(单独一行 " ")就等同于 `T 20260909 090000`,
  测试时不用每次手动敲完整的 `T YYYYMMDD HHMMSS` 命令

## Piezo VBR-Sen ver1.0.1 — 2026-09-07

- 修复 bug:标定期间(10s)`time:` 行末尾一直显示 `0.00V 0 0%`,是因为 `adcLastRaw/adcLastCentivolt`
  只在标定完成、进入 RUNNING 后才更新。改为标定期间也实时更新电压/原始值(百分比在标定完成前无意义,仍固定 0)

## Piezo VBR-Sen ver1.0.0 — 2026-09-07

- 产品/固件重新命名为 **Piezo VBR-Sen**,版本号从 `verMAJOR.MINOR.PATCH`(两位数字)方案
  重置为 `verX.Y.Z`,标志着这是一个新的产品线(纯模拟振动检测,不再兼容旧的数字脉冲协议)
- **完全移除 IO13 上的 SQ-SEN-200 数字振动传感器通道**(不再需要):`SENSOR_ENABLED`、
  数字环形缓冲、`onFalling` 中断、`aa sss.ssss s` 输出行等相关代码全部删除,现在只剩
  IO32 这一路 ADC 模拟通道
- `r`/`R` 的回应文案同步改为 `counter reset (ccc=1, buffer cleared)`(不再提"pulse")
- 心跳 LED 从 IO27 改到 **IO23**

## ver3.08.01 — 2026-09-07(历史版本,SQ-SEN-200 数字通道仍在,已移除)

- 修复 bug:新增的 raw 原始值需要额外读一次 ADC(analogRead),每次回调开销翻倍,
  之前的 200µs 默认值实机复测又触发 `task_wdt` 重启。重新二分法测出新硬上限在
  220~250µs 之间,`ADC_SAMPLE_US` 改为 400µs(2.5kHz),留出约 1.6 倍余量并复测通过

## ver3.08.00 — 2026-09-07

- ADC 事件行格式改为 `ccc tt.ttttS a.aaV vvvv xx%`,新增背景参考值:
  `a.aaV`=校准后电压(analogReadMilliVolts)、`vvvv`=原始 ADC 采样值(analogRead,0~4095)
- 每分钟 `time:` 播报行同步追加电压/原始值,格式变为 `time: ... a.aaV vvvv xx%`
- 百分比计算逻辑不变,仍然只用校准后电压;新增的原始值只读一次、纯展示用

## ver3.07.02 — 2026-09-07

- 实机二分法测出 ADC 采样定时器的看门狗硬上限:100/120µs 必炸 `task_wdt`,135µs 起才稳,
  硬上限落在 120~135µs(约 7.4~8.3kHz)之间且没有余量。默认 `ADC_SAMPLE_US` 改为 200µs(5kHz),
  留出约 1.5 倍安全余量(200/250/500µs/1kHz 均实测跑满 10s 标定无异常)
- `ADC_NOMINAL_IDLE_V` 按现场实测值改成 3.188V

## ver3.07.01 — 2026-09-07

- 修复 bug:ADC 采样定时器 100µs(10kHz)会把所在核心 CPU 占满,IDLE 任务喂不上看门狗,
  10 秒标定期间必然触发 `task_wdt` 导致重启(实机复现)。`ADC_SAMPLE_US` 改为 1000µs(1kHz)

## ver3.07.00 — 2026-09-07

- 新增 IO32 模拟振动通道(ADC1_CH4):`s`/`S` 同时启动该通道,先花 10s 标定"空闲高电平=0%"基线(均值-噪声半幅-余量),
  再用 esp_timer 硬件定时器以 100µs(10kHz)周期采样,只有幅度 >1% 才输出一行 `ccc tt.ttttS xx%`
  (ccc=计数器、tt.ttttS=距整分基准的秒偏移、xx%=幅度,100mV 以下固定算 100%)
- 标定出的空闲电压偏离期望值(`ADC_NOMINAL_IDLE_V`)超过 `ADC_DRIFT_WARN_V` 时,发一行英文 WARNING 提示(仍可用但异常)
- 每分钟 `time:` 播报行末尾追加当前 ADC 幅度百分比
- IO33(与 IO32 共用插头,现场接了 GND)设为纯输入,避免和地线对冲
- 详细规格见 [main.cpp](src/main.cpp) 顶部的需求规格注释

## ver3.06.00 — 2026-09-07

- 新增 `SENSOR_ENABLED` 编译开关(默认 **0**):关闭时 IO13 中断完全不挂载,只保留内部上拉,不采集任何脉冲;改回 `1` 并重新烧录即可恢复采集
- `IO13` 引脚由 `INPUT` 改为 `INPUT_PULLUP`,解决当前悬空导致的误触发问题

## ver3.05.00 — 2026-09-02

- 振动传感器输入脚 IO23 → **IO13**

## ver3.04.00 — 2026-09-01

- 振动传感器输入脚 IO33 → **IO23**

## ver3.03.00 — 2026-08-27

- `r`/`R` 回应加上当前时间:`pulse counter reset (aa=1, buffer cleared) @ 2026-08-27 11:23:45`(未设时钟则不带 `@`)

## ver3.02.00 — 2026-08-27

- `r`/`R` 改为**只复位脉冲计数 + 清空缓冲**,不再刷新时间锚点、不再补发 `time:` 行(时钟/基准完全不动)
- 回应改为 `pulse counter reset (aa=1, buffer cleared)`

## ver3.01.00 — 2026-08-27

- `s`/`S` 不再补发 `time:` 基准行(`start @` 行 + 下一整分的 `time:` 已足够;基准 = `start @` 时刻向下取整到整分)
- 脉冲计数 `aa` 从 **1** 开始(原来从 0):首个脉冲是 `01`

## ver3.00.00 — 2026-08-27

脉冲时间偏移的基准从"`s` 那一刻"改成"所在整分钟",输出格式随之改变(MAJOR)。

- 基准 = 每个脉冲所在的墙上整分(如 `2026-08-27 11:23:00`),与 `s` 何时按下无关
- `time:` 行(整分播报)就是基准;脉冲 = 距最近 `time:` 的偏移
- **脉冲格式** `aa ccc.cms` → **`aa sss.ssss s`**(秒,4 位小数 = 0.1ms)
  - 例:`s` 在 11:23:34,某脉冲在 11:23:38.2345 → `01 38.2345 s`(距 11:23:00 的偏移)
- ISR 用 `esp_timer` + 一个"esp_timer↔墙上时间"锚点推算每个脉冲的绝对时刻(不在 ISR 里调 `gettimeofday`)
- `s` 时先打 `start @ <确切时刻>`,再打一行 `time: <本整分>` 作为首个基准
- `time:` 整分行仅在发送缓冲清空时播报,保证上一分钟的脉冲都排在它前面
  (仅当持续每秒数百脉冲、且刚好跨整分时才可能错序,常规几百脉冲无影响)
- `r`/'R' 后补发一行 `time:` 让下位机重新对齐

## ver2.06.00 — 2026-08-27

- `?` 末尾加两行状态:`state: sensing` / `state: stopped`,以及 `clock: <时间>` / `clock: not set`

## ver2.05.00 — 2026-08-27

- `s`/`S` 现在要求**先设时钟**:未设时钟时拒绝启动,回 `clock not set, send 'T YYYYMMDD HHMMSS' before 's'`
- 因此 `start` 行必定带绝对时间(不再有 "clock not set" 分支)

## ver2.04.00 — 2026-08-27

- **修复** `time:` 播报失控(连续刷屏):旧逻辑用 `millis()` 滚动 60s + 单个 `if` 补偿,落后时会无符号下溢导致每个 loop 都打印
- 改为按**墙上时钟整分**触发一次(`epoch/60` 变化时打印),`ss` 基本为 `00`;结构上每分钟最多一行
- `time` 手动查询命令不变

## ver2.03.00 — 2026-08-27

- 每 60 秒的 `time:` 播报不再要求正在采集:只要设过时钟,空闲时也每分钟打印一次
- 播报节拍改为从 `T` 设时钟那一刻起算(`s`/`S` 不再重置节拍)

## ver2.02.00 — 2026-08-27

- 新命令 `time` → 打印当前墙上时间 `time: 2026-08-27 14:05:12`;未设置则回 `clock not set, send 'T YYYYMMDD HHMMSS'`

## ver2.01.00 — 2026-08-27

新增墙上时间支持,不影响 `aa ccc.cms` 脉冲格式。

- 新命令 `T YYYYMMDD HHMMSS`(如 `T 20260827 140000`)→ `settimeofday()` 设定时钟,回 `time set: 2026-08-27 14:00:00`
- 时区固定 `UTC0`,不做换算(输入几点就是几点)
- `s`/`S` 时多打一行:`start, t=0 @ 2026-08-27 14:00:03`(未设时钟则提示 `clock not set`)
- `s`/`S` 之后每 60 秒播报一行 `time: 2026-08-27 14:01:03`(需已设时钟)
- 断电即丢,重新上电需再发 `T`;晶振漂移约每天 1~3 秒,可定期重发校正

## ver2.00.00 — 2026-08-27

时间分辨率 1 ms → **0.1 ms**,输出格式随之改变(不兼容,故 MAJOR)。

- `TICK_US` 1000 → 100,时间戳内部单位变为 100µs(`uint32`,可存 ~5 天)
- 串口输出格式 `aa cccms` → **`aa ccc.cms`**(保留 1 位小数):
  - 第一个脉冲 `00 0.0ms`,5ms 时 `02 5.0ms`,123.4ms 时 `07 123.4ms`
  - 下位机解析器需处理小数点
- ISR / 缓冲 / 命令 / 带宽 均无实质变化;`esp_timer_get_time()` 本就是 1µs 源,抖动仅几 µs,0.1ms 有效

## ver1.00.00 — 2026-08-27

首个版本。SQ-SEN-200 振动传感器脉冲记录器。

- 振动信号接 GPIO33(推挽输入),下降沿中断捕获脉冲
- GPIO27 心跳 LED,0.5 s 翻转,用于判断 MCU 是否存活
- 时间戳分辨率 1 ms,相对「收到 `s` 的时刻」计时(uint32,49 天溢出)
- 环形缓冲 4096 条(每条 `uint32` 时间,16 KB),满则计入 `dropped`
- 串口 115200,输出格式 `aa cccms\n`(次数至少 2 位、时间至少 3 位,自动加宽)
- 发送非阻塞:串口无空间时留在缓冲下轮再发,不丢脉冲
- 无自动停止,只响应命令
- 命令:
  - `s`/`S` — 复位并开始采集,此刻为 t=0
  - `p`/`P` — 停止采集(缓冲继续发完)
  - `r`/`R` — 计数器归零(t=0、aa=0、清空缓冲),不改变启停状态
  - `?` — 打印版本号 + 命令列表
  - `ping` — 回 `pong`
